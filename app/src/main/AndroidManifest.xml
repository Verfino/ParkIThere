<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="OskarKowalik.pracaINZ">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PopUpActivity"
            android:theme = "@style/AppTheme.CustomTheme">
        </activity>
        <activity android:name=".ParkingLotViewActivity" />
        <activity android:name=".RegisterLoginActivity" />
        <activity android:name=".ParkingPositionViewActivity" />
        <activity android:name=".activity_parking_list" />
        <activity android:name=".ParkingListActivity" />
        <activity android:name=".activity_vehicle_listview" />
        <activity android:name=".VehicleListActivity" />
        <activity android:name=".CarSettingsActivity" />
        <activity android:name=".activity_setting_login_contact" />
        <activity android:name=".SettingsLoginActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".AfterLoginActivity" />
        <activity android:name=".loginActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>